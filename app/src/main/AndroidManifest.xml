<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cat.pianopatienttracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        tools:replace="android:supportsRtl">
        <activity android:name=".rep.home.patients.add_patient.PatientAddAfinitorActivity"></activity>
        <activity
            android:name=".login.LoginSsoWebview"
            android:screenOrientation="portrait" />
        <activity android:name=".rep.home.patients.add_patient.PatientAddPiqrayActivity" />
        <activity android:name=".rep.home.patients.PatientsActivity" />
        <activity android:name=".flm.Flm_home" />
        <activity
            android:name=".admin_manager_regional.progress.progress_flm.ProgressFlmActivity"
            android:screenOrientation="portrait" /> <!-- Intent filter to capture System Browser or Authenticator calling back to our app after sign-in -->
        <activity
            android:name=".rep.Rep_home"
            android:screenOrientation="portrait" />
        <activity
            android:name=".admin_manager_regional.Admin_home"
            android:screenOrientation="portrait" />
        <activity
            android:name=".login.LoginActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>